# Consumption-of-remote-resources
Mobile application that allows listing and managing contacts, applying the Domain-Driven Design (DDD) approach and using an ORM (Drift) for data persistence in an SQLite database.
